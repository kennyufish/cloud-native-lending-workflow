apiVersion: 1

providers:
  - name: Lending workflow
    orgId: 1
    folder: Lending workflow
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
