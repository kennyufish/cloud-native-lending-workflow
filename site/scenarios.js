/* Narrative snapshots of the Java sources, not a running backend. Text pairs: English, Chinese. */
const T = (en, zh) => [en, zh];
const step = (title, copy, active, flows, state, source) => ({ title, copy, active, flows, state, source });
const workflowSource = 'application-service/src/main/java/com/tszkinyu/lending/application/workflow/ApplicationWorkflow.java';
const publisherSource = 'application-service/src/main/java/com/tszkinyu/lending/application/outbox/OutboxPublisher.java';
const workerSource = 'decision-worker/src/main/java/com/tszkinyu/lending/decision/workflow/DecisionMessagePoller.java';
const policySource = 'decision-worker/src/main/java/com/tszkinyu/lending/decision/workflow/DecisionPolicy.java';
const lendingStart = [
  step(T('A request enters the system.', '一笔申请进入系统。'), T('A fictional applicant requests $25,000, with $120,000 annual income and a credit score of 760. The API validates the request and its idempotency key.', '虚构申请人：申请 $25,000，年收入 $120,000，信用分 760。API 验证请求及幂等键。'), ['client','api'], [['client','api']], [T('Application','申请'), T('Not saved yet','尚未保存')], workflowSource),
  step(T('Save it all, or save nothing.', '要么全部保存，要么全部回滚。'), T('One PostgreSQL transaction writes the application, an outbox event and an audit record. The API returns 201 / SUBMITTED; the decision happens asynchronously.', '在同一个 PostgreSQL 事务中写入申请、outbox 事件和审计记录。API 返回 201 / SUBMITTED，决策随后异步完成。'), ['api','db'], [['api','db']], [T('Application','申请'), 'SUBMITTED'], workflowSource),
  step(T('The outbox hands it over.', 'Outbox 把事件交给队列。'), T('The publisher claims a pending event with a lease, sends it to SQS and then marks it published. A failed send leaves it pending for another attempt.', 'Publisher 通过租约领取待发送事件，发给 SQS 后再标记为已发布。发送失败时，事件保持待处理，等待再次发送。'), ['db','outbox','queue'], [['db','outbox'],['outbox','queue']], [T('Delivery','投递'), T('At least once','至少一次')], publisherSource),
];
const evaluateStep = step(T('A small, explicit decision rule.', '执行清晰的演示决策规则。'), T('The worker receives the event. Score ≥ 680 and amount ≤ 35% of income pass the demo policy. This sample qualifies for 6.49% APR over 36 months.', 'Worker 读取事件。演示策略要求信用分 ≥ 680，申请金额 ≤ 年收入的 35%。本例符合条件，得到 6.49% 年利率、36 个月期限。'), ['queue','worker'], [['queue','worker']], [T('Decision','决策'), 'APPROVED'], policySource);
const callbackStep = step(T('Record the outcome once.', '结果只落库一次。'), T('The worker calls the internal API. A single transaction binds eventId to applicationId, records the offer and appends audit events. Notification is simulated.', 'Worker 调用内部 API。一个事务将 eventId 与 applicationId 绑定，记录 offer，并追加审计事件。通知仅为模拟记录。'), ['worker','api','db'], [['worker','api'],['api','db']], [T('Application','申请'), 'OFFERED'], workflowSource);
const finishStep = step(T('Only then, acknowledge.', '落库成功后，才确认消息。'), T('The worker deletes the SQS message only after the callback succeeds. A client can now retrieve OFFERED, the offer terms and the audit timeline.', '只有回调成功，Worker 才删除 SQS 消息。客户端现在可以查询 OFFERED 状态、offer 条件和完整审计时间线。'), ['worker','queue','result'], [['worker','queue']], [T('Notification','通知'), 'SIMULATED'], workerSource);

globalThis.DEMO = {
    name:'Lending Workflow',repo:'cloud-native-lending-workflow',revision:'2093674c8ca3ef7911a213e69a0adbcd49826434',
    label:T('CLOUD NATIVE LENDING WORKFLOW','云原生贷款申请流程'),
    title:T('One application. <span>A traceable journey.</span>','一笔申请，<span>全程有迹可循。</span>'),
    intro:T('Follow an application through two Java services. See how the system handles success, repetition and failure.','跟随一笔申请穿过两个 Java 服务，观察成功、重复提交和失败时系统如何处理。'),
    tech:'Java 21 / Spring Boot / PostgreSQL / SQS',
    boundary:T('A scripted walkthrough of repository behavior. Fictional applicant data and deterministic demo policy; notifications are simulated. Local validation uses LocalStack. This is not an AWS deployment.','根据仓库行为编写的动画。申请数据虚构，决策为确定性演示策略，通知为模拟记录。本地验证使用 LocalStack，不代表已经部署到 AWS。'),
    scenarios:{
      success:{name:T('Happy path','成功流程'),title:T('From submitted to offered.','从提交申请到生成 offer。'),kind:'lending',steps:[...lendingStart,evaluateStep,callbackStep,finishStep],footer:T('Fictional sample · $25,000 request / $120,000 income / score 760','虚构示例 · 申请 $25,000 / 年收入 $120,000 / 信用分 760')},
      replay:{name:T('Duplicate request','重复请求'),title:T('Same request. Same application.','同一请求，返回同一申请。'),kind:'lending',steps:[...lendingStart.slice(0,2),
        step(T('The client sends it again.', '客户端再次提交。'),T('Imagine the client missed the first response and repeats the same payload with the same Idempotency-Key. The database already owns that key.', '假设客户端没收到首次响应，使用同一个 Idempotency-Key 重发相同内容。数据库中已存在这个键。'),['client','api'],[['client','api']],[T('Request','请求'),T('Same key + payload','相同键与内容')],workflowSource),
        step(T('The unique key catches the repeat.', '唯一键识别出重复提交。'),T('INSERT … ON CONFLICT does not create a second application. The service looks up the existing row and compares the stored request hash.', 'INSERT … ON CONFLICT 不会创建第二笔申请。服务查询已有记录，并比较已保存的请求哈希。'),['api','db'],[['api','db']],[T('Application rows','申请记录数'),'1'],workflowSource),
        step(T('Return the existing application.', '返回已经存在的申请。'),T('A matching payload returns HTTP 200, the same application ID and replayed: true. No additional outbox event or submission audit record is created. The original event continues independently.', '内容一致时，返回 HTTP 200、相同申请 ID 和 replayed: true。不新增 outbox 事件或提交审计记录。原事件继续独立处理。'),['db','api','client'],[['db','api'],['api','client']],[T('Replay result','重放结果'),'200 / replayed: true'],workflowSource),
        step(T('Different payload? Reject it.', '同一个键换了内容？拒绝。'),T('A separate branch: reuse this key with a changed amount and the request hash no longer matches. The service responds with HTTP 409. The existing application remains intact.', '另一个分支：如果复用这个键，却改变申请金额，请求哈希将不匹配。服务返回 HTTP 409，保留原申请。'),['client','api','db'],[['client','api'],['api','db']],[T('Changed payload','内容改变'),'409 CONFLICT'],workflowSource),
      ],footer:T('Request replay and conflicting-payload branch · original event continues','请求重放与内容冲突分支 · 原事件继续异步处理')},
      retry:{name:T('Callback retry','回调重试'),title:T('A lost response must not duplicate an offer.','响应丢失，也不能重复生成 offer。'),kind:'lending',steps:[...lendingStart,evaluateStep,callbackStep,
        step(T('Saved, but the response is lost.', '已经落库，但响应丢失。'),T('In this failure scenario the API committed the decision, but the worker times out waiting for its response. The worker does not delete the SQS message.', '本故障场景中，API 已提交决策，但 Worker 等待响应超时。Worker 不会删除 SQS 消息。'),['api','worker','queue'],[],[T('Worker acknowledgement','Worker 确认'),T('Not received','未收到')],workerSource),
        step(T('SQS makes the message visible again.', 'SQS 让消息重新可见。'),T('After the visibility timeout, the worker can receive the same event again and repeat the callback. Local Compose uses 5 seconds; this animation compresses the wait.', '可见性超时后，Worker 可以再次读取相同事件并重复回调。本地 Compose 配置为 5 秒；动画压缩了等待时间。'),['queue','worker','api'],[['queue','worker'],['worker','api']],[T('Event ID','事件 ID'),T('Unchanged','保持不变')],workerSource),
        step(T('Deduplicate at the transaction.', '在数据库事务中去重。'),T('processed_events already contains this event ID. Matching application ID and decision hash return the existing result without repeating the offer or audit writes. A mismatched decision would be rejected.', 'processed_events 已包含此事件 ID。申请 ID 和决策哈希匹配时，直接返回已有结果，不重复写入 offer 或审计记录。不同的决策则会被拒绝。'),['api','db'],[['api','db']],[T('Offer side effect','Offer 副作用'),T('Recorded once','仅记录一次')],workflowSource),finishStep,
      ],footer:T('Failure scenario: decision committed, callback response lost','故障情景：决策已提交，但回调响应丢失')},
      dlq:{name:T('Dead-letter queue','死信队列'),title:T('Isolate an event that cannot be processed.','隔离始终无法处理的事件。'),kind:'lending',steps:[
        step(T('Inject a malformed event.', '注入格式错误的事件。'),T('This is the repository’s failure-drill path: a malformed message is sent directly to the queue. It does not come from the validated application API.', '这是仓库的故障演练路径：直接向队列发送格式错误的消息。它并非来自通过验证的申请 API。'),['queue'],[],[T('Message','消息'),T('Malformed payload','错误格式内容')],workerSource),
        step(T('Parsing fails. Do not acknowledge.', '解析失败，不确认消息。'),T('The worker cannot read a valid application event, so it records a failure. No callback succeeds and no DeleteMessage is issued.', 'Worker 无法解析有效的申请事件，因此记录失败。回调未成功，也不会执行 DeleteMessage。'),['queue','worker'],[['queue','worker']],[T('Receive count','接收次数'),'1'],workerSource),
        step(T('Retry after visibility expires.', '可见性超时后重试。'),T('The message becomes available again after the timeout. The same invalid payload still fails; merely retrying cannot fix its contents.', '超时后消息再次可用。相同的错误内容仍会失败；单纯重试无法修复消息内容。'),['queue','worker'],[['queue','worker']],[T('Receive count','接收次数'),'2'],workerSource),
        step(T('Reach the local retry threshold.', '达到本地重试阈值。'),T('LocalStack’s queue is configured with maxReceiveCount = 3. After repeated failures reach the redrive threshold, SQS moves the message to the DLQ. Terraform’s AWS shape uses 5.', 'LocalStack 队列配置 maxReceiveCount = 3。反复失败达到重定向阈值后，SQS 将消息移入 DLQ。Terraform 的 AWS 配置使用 5。'),['queue','worker'],[['queue','worker']],[T('Receive count','接收次数'),'3'],'docker/localstack/init/ready.d/10-create-queues.sh'),
        step(T('Keep it for investigation.', '保留消息，等待排查。'),T('The dead-letter queue isolates the bad message. Operators can inspect and fix the cause before a controlled redrive. Moving to the DLQ does not generate an offer.', '死信队列隔离问题消息。排查并修复原因后，可以受控地重新投递。进入 DLQ 不会生成 offer。'),['queue','dlq'],[['queue','dlq']],[T('Outcome','结果'),'DLQ'], 'docs/OPERATIONS.md'),
      ],footer:T('Local failure drill · three receives · no application or offer created','本地故障演练 · 三次接收 · 不创建申请或 offer')},
    },
    nodes:[['client',T('Client','客户端'),'Idempotency-Key'],['api',T('Application API','申请 API'),'Spring Boot'],['db','PostgreSQL',T('app + outbox + audit','申请 + outbox + 审计')],['outbox',T('Outbox publisher','Outbox 发布器'),T('claim → send → mark','领取 → 发送 → 标记')],['queue','SQS',T('at-least-once delivery','至少一次投递')],['worker',T('Decision worker','决策 Worker'),T('policy + callback','策略 + 回调')],['result',T('Offer + audit','Offer 与审计'),'OFFERED · SIMULATED'],['dlq',T('Dead-letter queue','死信队列'),T('inspect before redrive','排查后重新投递')]],
    evidence:[{label:T('Persistence boundary','持久化边界'),value:T('One transaction','一个事务'),unit:'',note:T('Application + outbox event + audit record','申请 + outbox 事件 + 审计记录')},{label:T('Delivery guarantee','消息投递语义'),value:T('At least once','至少一次'),unit:'',note:T('Event ID + decision hash deduplicate side effects','事件 ID + 决策哈希，对副作用去重')}],
  };
